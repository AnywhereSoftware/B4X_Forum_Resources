AppType=JavaFX
Build1=Default,b4j.example
File1=Layout1.bjl
FileGroup1=Default Group
Group=Default Group
Library1=jcore
Library2=jfx
Library3=jxui
Library4=jsysteminformation
NumberOfFiles=1
NumberOfLibraries=4
NumberOfModules=0
Version=10
@EndOfDesignText@
#Region Project Attributes 
	#MainFormWidth: 600
	#MainFormHeight: 600 
	#AdditionalJar : jackson-annotations-2.17.2
	#AdditionalJar : jackson-core-2.17.2
	#AdditionalJar : jackson-databind-2.17.2

#End Region

Sub Process_Globals
	Private fx As JFX
	Private MainForm As Form
	Private xui As XUI 
   
    Dim sysinfo As jSystemInformation
	Private Button1 As Button
End Sub

Sub AppStart (Form1 As Form, Args() As String)
	MainForm = Form1
	MainForm.RootPane.LoadLayout("Layout1")
	MainForm.Show
	
	

End Sub

Private Sub Button1_Click
	
	Log(sysinfo.OS)
	Log(sysinfo.CPU)
	Log(sysinfo.Disk)
	Log(sysinfo.Memory)
	Log(sysinfo.Net)
	Log(sysinfo.ListOfProcess)

End Sub