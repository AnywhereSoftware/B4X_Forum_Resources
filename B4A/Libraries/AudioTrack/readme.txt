Extract the AudioRecord.jar and AudioRecord.xml files to your additional library directory.

This is an Audio Recording Library based on the Android AudioRecord object which allows capturing of sound input to a data file for further processing.

Included in the demo program is a WAV file header routine that allows saving captured sound as an uncompressed wav file.